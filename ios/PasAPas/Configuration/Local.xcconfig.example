// Copy to Local.xcconfig (ignored by Git) and set real deployed HTTPS origins.
// Xcode's $() escape prevents // from being parsed as a comment.
PASAPAS_STAGING_API_BASE_URL = https:/$()/staging.your-domain.example
PASAPAS_PRODUCTION_API_BASE_URL = https:/$()/your-domain.example
