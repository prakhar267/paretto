// Safe tracked defaults for every app build configuration. A developer or CI
// environment may provide real deployment origins in the ignored local file.
LOQUIVO_STAGING_API_BASE_URL =
LOQUIVO_PRODUCTION_API_BASE_URL =

#include? "Local.xcconfig"
